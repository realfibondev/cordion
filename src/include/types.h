#pragma once

// in the API it's a string, so it will automatically get converted to "unsigned long" in all the convert functions
typedef unsigned long snowflake;
typedef unsigned long timestamp_t;

typedef const char* string;
typedef string nullable_string;
typedef unsigned long ul_t;

typedef string hex;
